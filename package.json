{
  "name": "arrse",
  "version": "1.0.0",
  "private": true,
  "description": "A TypeScript Intel hardware media optimizer for Sonarr and Radarr",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/index.js",
    "dev": "node --enable-source-maps dist/src/index.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^7.0.2"
  }
}
